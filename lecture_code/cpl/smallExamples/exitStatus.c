#include <stdio.h>
int main() {
  int x, y;
  /*
   * Once again, don't worry about this code for now.
   * Just know this program reads two integers from standard input
   * adds them together and exits with the sum as the status code
   */
  scanf("%d", &x);
  scanf("%d", &y);
  return x+y;
}
